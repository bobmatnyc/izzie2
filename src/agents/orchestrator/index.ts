/**
 * Orchestrator Agent (Claude Opus)
 * Main decision-making agent that coordinates other agents
 */

import type { AgentContext } from '@/types';

export class OrchestratorAgent {
  constructor() {
    // Placeholder for POC-1 (Issue #8)
  }

  async process(context: AgentContext): Promise<void> {
    // TODO: Implement in POC-1
    console.warn('OrchestratorAgent.process not yet implemented', context);
  }
}
